setwcs.c
  Automaticaly find stars in a FITS or IRAF image, match them to HST Guide Stars,
  compute the relation between sky coordinates and image coordinates, and write
  in in the image header. 
delwcs.c
  Delete the WCS keywords from an image. If both EPOCH and EQUINOX are
  present, EQUINOX is also deleted. 
imstar.c
  Find and list the n brightest stars in an IRAF or FITS image, with their sky
  coordinates if there is WCS information in the image header. 
xy2sky.c
  Print sky coordinates for given image pixel coordinates on the command line or
  in a list file. 
sky2xy.c
  Print image pixel coordinates for given sky coordinates on the command line or
  in a list file. 
